
{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
    <h1>{{ "ideas.update"|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" class="btn btn-primary" value="Save" />
    {{ form_end(form) }}

</div>
{% endblock %}
